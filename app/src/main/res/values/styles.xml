<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="InfoTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="InfoTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorBackground">@color/base2</item>
        <item name="colorPrimary">@color/base3</item>
        <item name="colorPrimaryDark">@color/base00</item>
        <item name="colorAccent">@color/blue</item>
    </style>

</resources>
